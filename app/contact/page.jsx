import ContactClient from "./contactClient";

export const metadata = {
  title: "Contact | Reavyn Tech Alliance",
 };

export default function contactPage() {
  return (
    <main>
      <ContactClient />
    </main>
  );
}
